# QES Workshop

React workshop app for the challenge exercises.

## Run locally

```sh
npm install
npm run dev
```

Open the local Vite URL, choose a challenge, and edit the challenge's `exercise.tsx` file. The app keeps the instructions, hints, checks, and flag reveal separate so the exercise file stays focused on the code you need to change.
